### htcacheclean settings ###

## run htcacheclean: yes, no, auto
## auto means run if /etc/apache2/mods-enabled/disk_cache.load exists
## default: auto
HTCACHECLEAN_RUN=auto

## run mode: cron, daemon
## run in daemon mode or as daily cron job
## default: daemon
HTCACHECLEAN_MODE=daemon

## cache size 
HTCACHECLEAN_SIZE=<%= node[:apache][:disk_cache][:htcacheclean_size] %>

## interval: if in daemon mode, clean cache every x minutes
HTCACHECLEAN_DAEMON_INTERVAL=<%= node[:apache][:disk_cache][:htcacheclean_interval] %>

## path to cache
## must be the same as in CacheRoot directive
HTCACHECLEAN_PATH=<%= node[:apache][:cache_dir] %>/mod_disk_cache

## additional options:
## -n : be nice
## -t : remove empty directories
HTCACHECLEAN_OPTIONS="<%= node[:apache][:disk_cache][:htcacheclean_options] %>"
